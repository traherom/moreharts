/*********************
Main logic of program.
*********************/
void main(void)
{
	int test1; // Should be an error/warning (num folling id)
	int 1anArray[20]; // id following num
	int goal = 10;

	// We could add these one line comments :)

	if(test == 0)
	{
		other();
	}
	else
		someOther();

	/* Testing looping. */
	test = 2;
	while(test <= goal)
	{
		test++;
	}

	ret; // should just be an id (not beginning of "return")
	whi2; // should come out as id followed by num error
	while2; // should error as well

	return1; // should be an error
}
